/**
 * Testclass for running the robot simulator.
 * To run the simulator, input from user should be the name of a text file in the directory commandFiles.
 *
 * @author Klara Näslund
 */

public class TestClass {

    public static void main(String[] args) {
        RobotSimulator robotSimulator = new RobotSimulator();
        robotSimulator.run();
    }
}
